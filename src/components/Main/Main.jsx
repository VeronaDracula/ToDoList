import React, {useState} from 'react';

import styles from './Main.module.scss';

function Main() {
    return (
        <main className={styles.container}>
        </main>
    );
}

export default Main;
